using System;
using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class HealthBarDefiner : MonoBehaviour
{
    // Start is called before the first frame update
    /*public float healthRatio;
    public SpriteRenderer healthBar;

    public HealthBarDefiner instance;

    private void Awake()
    {
        instance = this;
    }

    public void HealthBarAdjustment()
    {
        if (healthRatio >= 0)
        {
            healthBar.size = new Vector2(healthRatio, 1f);
        }
    }*/
}
